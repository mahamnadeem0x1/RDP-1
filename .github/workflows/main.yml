name: Ubuntu CI Secure

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      MY_PASS: "mahamSK=-12"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show system info
        run: |
          uname -a

      - name: Use password safely
        run: |
          echo "Password is set (not printing for security)"
          # Example usage:
          # some_command --password "$MY_PASS"
